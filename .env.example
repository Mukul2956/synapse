# ════════════════════════════════════════════════════════════════════════════
#  ORBIT  –  Environment Configuration
#  Copy this file to .env and fill in every value before running the app.
#  NEVER commit your actual .env to git.
# ════════════════════════════════════════════════════════════════════════════

# ─── Supabase / PostgreSQL ───────────────────────────────────────────────────
# HOW TO GET:
#   1. Go to https://supabase.com → New Project → wait for it to be ready
#   2. Left sidebar → Settings → Database
#   3. Scroll to "Connection string" → select "URI" tab
#   4. Copy the string (it looks like the examples below)
#      Replace [YOUR-PASSWORD] with the password you set for the project.
#
# DATABASE_URL   → used by the FastAPI app at runtime  (asyncpg async driver)
# SYNC_DATABASE_URL → used ONLY by Alembic migrations (psycopg2 sync driver)
#
DATABASE_URL=postgresql+asyncpg://postgres.[YOUR-PROJECT-REF]:[YOUR-DB-PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres
SYNC_DATABASE_URL=postgresql+psycopg2://postgres.[YOUR-PROJECT-REF]:[YOUR-DB-PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres

# ─── Redis (Celery broker + result backend) ──────────────────────────────────
# HOW TO GET (free):
#   Option A – Upstash (recommended, serverless):
#     1. https://upstash.com → Redis → Create Database → choose a region
#     2. Dashboard → your DB → "Connect" tab → copy the redis:// URL
#   Option B – Redis Cloud free tier: https://redis.com/try-free/
#
#   Format: redis://default:[password]@[hostname]:[port]
#
REDIS_URL=redis://default:[YOUR-REDIS-PASSWORD]@[YOUR-REDIS-HOST]:[PORT]
CELERY_BROKER_URL=redis://default:[YOUR-REDIS-PASSWORD]@[YOUR-REDIS-HOST]:[PORT]/0
CELERY_RESULT_BACKEND=redis://default:[YOUR-REDIS-PASSWORD]@[YOUR-REDIS-HOST]:[PORT]/1

# ─── App Security ────────────────────────────────────────────────────────────
# HOW TO GET: Generate a random secret once and paste it here.
#   Run in Python:  import secrets; print(secrets.token_hex(32))
#
SECRET_KEY=change-me-generate-with-python-secrets-token-hex-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ─── Reddit ──────────────────────────────────────────────────────────────────
# HOW TO GET:
#   1. Go to https://www.reddit.com/prefs/apps while logged in
#   2. Click "create another app" at the bottom
#   3. Choose type: "script" (for personal use / bots)
#   4. Set redirect URI to: http://localhost:8000
#   5. After creation:
#      - App ID (under the app name)   → REDDIT_CLIENT_ID
#      - Secret                         → REDDIT_CLIENT_SECRET
#   6. REDDIT_USERNAME / REDDIT_PASSWORD = your Reddit account login
#   NOTE: The account must be at least 30 days old for most subreddits.
#
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USERNAME=
REDDIT_PASSWORD=
REDDIT_USER_AGENT=ORBIT/1.0

# ─── LinkedIn ────────────────────────────────────────────────────────────────
# HOW TO GET:
#   1. https://www.linkedin.com/developers/apps → Create App
#   2. Auth tab:
#      - Client ID     → LINKEDIN_CLIENT_ID
#      - Client Secret → LINKEDIN_CLIENT_SECRET
#   3. Products tab: request access to "Share on LinkedIn" and "Sign In with LinkedIn"
#   NOTE: You need to do OAuth2 at runtime to get a user access token.
#         For hackathon demo, you can manually generate one via:
#         https://www.linkedin.com/developers/tools/oauth/token-generator
#
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=

# ─── YouTube / Google ────────────────────────────────────────────────────────
# HOW TO GET:
#   1. https://console.cloud.google.com → Create/select a project
#   2. APIs & Services → Library → search "YouTube Data API v3" → Enable it
#   3. APIs & Services → Credentials:
#      a. "+ Create Credentials" → OAuth 2.0 Client ID → Web Application
#         → YOUTUBE_CLIENT_ID / YOUTUBE_CLIENT_SECRET
#      b. "+ Create Credentials" → API Key → YOUTUBE_API_KEY
#   NOTE: For hackathon, YouTube publishing requires video upload (large scope).
#         You can leave these blank and skip YouTube in initial testing.
#
YOUTUBE_CLIENT_ID=
YOUTUBE_CLIENT_SECRET=
YOUTUBE_API_KEY=

# ─── App Config (safe to leave as-is) ────────────────────────────────────────
ENVIRONMENT=development
DEFAULT_TIMEZONE=UTC
MODEL_PATH=./models
PUBLISH_RETRY_ATTEMPTS=3
LOG_LEVEL=INFO

## youtube, reddit, linkedin.